{
  "name": "urlzy-url-shortener",
  "version": "1.0.0",
  "description": "A URL shortening service.",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "export NODE_ENV=development || SET \"NODE_ENV=development\" && node src/server.js",
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha src/**/*.test.js",
    "watch": "nodemon --exec npm run"
  },
  "author": "Fredrik Eklööf",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "express-handlebars": "^3.0.0",
    "mongoose": "^5.2.6",
    "validator": "^10.5.0"
  }
}
